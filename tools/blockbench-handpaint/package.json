{
  "name": "reborn-handpainted",
  "version": "0.1.0",
  "private": true,
  "description": "Blockbench plugin — hand-painted texturing suite for Reborn (AO, shade, lighting, gradient, edges, surfaces)",
  "type": "module",
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "blockbench-types": "^4.10.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
