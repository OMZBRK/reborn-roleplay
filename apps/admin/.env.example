# URL de base de l'API NestJS (inclut le prefix /v1).
# Cote serveur Next : utilise tel quel. Cote client : meme valeur expose via
# NEXT_PUBLIC_API_BASE_URL.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/v1
