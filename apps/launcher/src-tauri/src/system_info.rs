// Module deplace dans `hardware/mod.rs` (qui etait deja declare dans
// lib.rs comme placeholder). Ce fichier n'est reference nulle part et
// n'est donc pas compile — il restera ici inerte jusqu'au prochain
// menage manuel (la sandbox de l'agent ne peut pas le supprimer).
