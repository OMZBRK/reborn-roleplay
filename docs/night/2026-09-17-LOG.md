# Journal de nuit autonome — 2026-09-17 → 18

Boucle de travail autonome (auto-planifiée). Branche : `explore/overnight-2026-09-17`.
Chaque itération : une tâche du backlog → commit → mise à jour de ce journal.

## 🔒 Garde-fous
- **Aucun déploiement prod** (pas de SFTP / manifest / VPS). Correctifs = préparés + documentés.
- Chaque commit vérifié (build/tsc/self-test).
- Rien de lourd/risqué sans filet.

## Backlog (rotation)
- [A] **Spells de test visuels** : techniques variées (graph.json) + compilation + galerie HTML (graphe + YAML généré). But : base visuelle de ce que fait l'éditeur.
- [B] **Explorer les concurrents** : serveurs RP Naruto MC, systèmes de jutsu, éditeurs de sorts/nodes → doc d'idées.
- [C] **Correctifs préparés** : scan des sorts serveur (crash particule-sans-material + autres) → rapport + patchs prêts (non déployés).
- [D] **Idées de features** priorisées pour le Technique Creator + serveur.
- [E] **Améliorations éditeur** sûres (keyframes entity, préfixe anti-collision à l'import, etc.).

## Itérations

<!-- chaque itération s'ajoute ici -->

### Itération 1 — [A] Spells de test visuels + galerie
- Créé 4 techniques de démo variées dans `packages/ability-compiler/graphs/showcase/` :
  **Rasengan** (orbe item_display animé + area Pain), **Chidori** (ParticleProjectile foudre → Pain), **Shunshin** (LeapSpell + trail fumée), **Paume Mystique** (PotionEffect regen + aura soin). Couvre : entity animé, projectile, mobilité, buff/soin.
- Compilent toutes (4 techniques → 13 sorts MS + 1 magic-item).
- Générateur de galerie HTML `scripts/gallery.mts` (graphe SVG + YAML MS + abilities.yml par technique), régénérable.
- **Galerie publiée** : https://claude.ai/code/artifact/4f1ec9d2-6d41-4fac-a168-f9545ddfea6c (mise à jour à la même URL au fil de la nuit).
- Vérifs : compile OK.

### Itération 2 — [C] Correctifs préparés (scan sorts serveur)
- Scan de tous les `spells-*.yml` serveur pour le crash `BlockData` (particule block/item sans `material`).
- **Résultat** : aucune particule fautive dans les fichiers captés (9 particules utilisées, aucune famille BlockData) → le sort fautif est hors snapshot, **à localiser live** (commande grep fournie).
- **Prévention confirmée** : l'éditeur bloque déjà ce crash à la compilation.
- **Autre bug repéré** : construct YAML fragile dans `spells-command.yml` (chaîne multi-ligne guillemetée, continuation col 0) — patch prêt.
- Rapport : `docs/night/2026-09-17-correctifs-prepares.md`. **Rien déployé.**

### Itération 3 — [B] Concurrents & idées
- Veille : mods/serveurs Naruto (AHZNB ShinobiCraft, N-U-RP, Daikage) + outils d'édition MythicMobs (MMGUI, MythicMobsCreator, ext. VS Code).
- Constat clé : aucun outil concurrent ne combine **graphe visuel + MagicSpells-natif + preview en jeu + gate ShinobiCore** → notre edge.
- 7 pistes tirées : nœud « signes de main » (CLICK_SEQUENCE), **Kawarimi** (futur spell démo), sensing de chakra, palette teintée par nature, **bibliothèque de templates de nœuds**, jutsu par clan, suite mouvement shinobi.
- Doc : `docs/night/2026-09-17-concurrents-idees.md` (cité).

### Itération 4 — [D] Idées priorisées
- Roadmap impact/effort du Technique Creator (`docs/night/2026-09-17-idees-priorisees.md`).
- Quick wins : préfixe anti-collision import, **bibliothèque de templates de nœuds**, palette teintée par nature, sélecteur de fichier serveur à l'import.
- Moyen : éditeur keyframes entity, vue bibliothèque de sorts, nœud signes de main, validation enrichie.
- Ambitieux : Kawarimi, déploiement 1-clic incrémental, suite mouvement shinobi.
- Ordre conseillé pour les 57 techniques : templates → palette nature → sélecteur fichier → keyframes.
