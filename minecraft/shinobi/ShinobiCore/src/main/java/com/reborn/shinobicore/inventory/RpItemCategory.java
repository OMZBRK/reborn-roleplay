package com.reborn.shinobicore.inventory;

/** Catégories d'objets du sac RP — noms alignés sur l'enum client (mod-hud). */
public enum RpItemCategory {
    ARMES,
    CONSOMMABLES,
    PARCHEMINS,
    MATERIAUX,
    COSMETIQUES,
    SACS,
    QUETE
}
